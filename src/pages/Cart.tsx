import React from 'react';
import { Header, MainWrapper } from '../components';

const Cart = () => {
    return (
        <>
            <Header />
            <MainWrapper title="Cart"></MainWrapper>
        </>
    );
};

export default Cart;